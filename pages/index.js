import LoginForm from "@/components/forms/LoginForm";
import { Box } from "@mui/joy";

const LoginPage = () => {
  return (
    <Box>
      <LoginForm />;
    </Box>
  );
};

export default LoginPage;
